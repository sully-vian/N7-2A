# TP2 - RMI

On peut ajouter/consulter des fiches en ligne. Chacun des deux carnets relaie la requête à l'autre s'il n'a pas la fiche demandée.

Pour lancer les carnets:

```bash
java CarnetImpl
```

Pour lancer l'interface graphique:

```bash
java CarnetClient
```
